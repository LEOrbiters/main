name: leo_orbiters
description: LEO satellite collision alert system
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  latlong2: ^0.9.1
  http: ^1.2.2
  provider: ^6.1.2
  google_maps_flutter: ^2.6.1
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/.env